import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GestaoprontuariosPage } from './gestaoprontuarios.page';

describe('GestaoprontuariosPage', () => {
  let component: GestaoprontuariosPage;
  let fixture: ComponentFixture<GestaoprontuariosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GestaoprontuariosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
